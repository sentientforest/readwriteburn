# ReadWriteBurn Server Configuration

# Database
DB_FILE=readwriteburn.db

# Chaincode API
CHAIN_API=http://localhost:3000

# Vote Counting Service Configuration
VOTE_COUNTING_ENABLED=true                # Enable/disable automatic vote counting
VOTE_COUNTING_INTERVAL_MS=30000          # How often to check for uncounted votes (30 seconds)
VOTE_BATCH_SIZE=100                      # How many votes to process per batch (max 1000)
VOTE_MAX_PAGES_PER_RUN=10               # Maximum pages to process per interval run

# Server
PORT=4000
PROJECT_ID=ReadWriteBurn